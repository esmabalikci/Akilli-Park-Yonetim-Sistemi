DB_USER=apays_user
DB_PASSWORD=your_password_here
DB_SERVER=localhost
DB_PORT=1433
DB_DATABASE=apays_db
DB_ENCRYPT=false
JWT_SECRET=change-this-to-a-long-random-secret
JWT_EXPIRES=7d
RESERVATION_FEE_TRY=50

# Sunucu başlarken eksik tablolar otomatik oluşturulur (Events, LostFoundItems, Notifications).
# İlk kurulum için database_schema.sql dosyasını SQL Server'da çalıştırın.
